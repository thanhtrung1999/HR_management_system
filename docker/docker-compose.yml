version: "3.9"

#NETWORK
networks:
  my-network:
    driver: bridge

services:
  # container webserver
  web:
    container_name: webserver
    build:
      dockerfile: C:\xampp\htdocs\HR_management_system\docker\dockerfile\web.dockerfile
      context: ../.
    hostname: web
    restart: always
    networks:
      - my-network
    volumes:
      - ../.:/var/www/html/hr_management_system
      - ./config/000-default.conf:/etc/apache2/sites-enabled/000-default.conf
    ports:
      - "8888:80"
      - "443:443"
    links:
      - mysql
  # container db
  mysql:
    container_name: db
    image: mysql:latest
    hostname: mysql
    restart: always
    networks:
      - my-network
    volumes:
      - ./mysql/db/:/var/lib/mysql
      - ./config/my.cnf:/etc/mysql/my.cnf
    environment:
      - MYSQL_ROOT_PASSWORD=root@123
      - MYSQL_DATABASE=hr_management_system
      - MYSQL_USER=member
      - MYSQL_PASSWORD=member@123
    ports:
      - "3307:3306"
